# 桌面 app 打包专用依赖（与运行时 requirements.txt 分开，避免污染服务器/CI 环境）
# 安装：pip install -r requirements.txt -r requirements-build.txt
-r requirements.txt

# 原生窗口（Mac 用系统 WKWebView，无需额外浏览器）
pywebview>=5.0

# 打包为 .app / .exe
pyinstaller>=6.0
